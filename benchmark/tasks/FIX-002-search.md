# FIX-002：任务搜索结果不完整

## 问题描述

用户搜索任务时发现，只有部分结果能够被找到。产品要求搜索同时覆盖任务标题和描述，并且不区分大小写。

## 期望行为

- `query` 同时匹配 `title` 和 `description`。
- 匹配不区分大小写。
- 搜索后的 `total` 是匹配结果总数。
- 搜索仍然可以与状态过滤、分页组合使用。
- 不改变已有 API 响应结构。

## 验收命令

```bash
pytest -q grader_tests/test_fix_002_search.py
```

