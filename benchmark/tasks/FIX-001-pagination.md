# FIX-001：任务分页出现重复数据

## 问题描述

调用任务列表接口翻页时，相邻页面偶尔会出现同一条任务。例如每页两条时，第 1 页最后一条任务会再次出现在第 2 页。

## 期望行为

- 页码从 1 开始。
- 任意两个相邻页面不能出现重复任务。
- `total` 始终表示过滤后的任务总数，不受当前页影响。
- 最后一页可以少于 `page_size` 条。
- 保持现有 API 参数及响应结构兼容。

## 验收命令

```bash
pytest -q grader_tests/test_fix_001_pagination.py
```

